import React from 'react';

const User = (props) => {
    return (
        <div>
             <li key = {user.id}>
                    {user.username}
                </li>)
            
        </div>
    );
};

export default User;